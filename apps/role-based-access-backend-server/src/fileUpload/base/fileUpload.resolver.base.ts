/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { FileUpload } from "./FileUpload";
import { FileUploadCountArgs } from "./FileUploadCountArgs";
import { FileUploadFindManyArgs } from "./FileUploadFindManyArgs";
import { FileUploadFindUniqueArgs } from "./FileUploadFindUniqueArgs";
import { CreateFileUploadArgs } from "./CreateFileUploadArgs";
import { UpdateFileUploadArgs } from "./UpdateFileUploadArgs";
import { DeleteFileUploadArgs } from "./DeleteFileUploadArgs";
import { User } from "../../user/base/User";
import { FileUploadService } from "../fileUpload.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => FileUpload)
export class FileUploadResolverBase {
  constructor(
    protected readonly service: FileUploadService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "read",
    possession: "any",
  })
  async _fileUploadsMeta(
    @graphql.Args() args: FileUploadCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [FileUpload])
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "read",
    possession: "any",
  })
  async fileUploads(
    @graphql.Args() args: FileUploadFindManyArgs
  ): Promise<FileUpload[]> {
    return this.service.fileUploads(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => FileUpload, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "read",
    possession: "own",
  })
  async fileUpload(
    @graphql.Args() args: FileUploadFindUniqueArgs
  ): Promise<FileUpload | null> {
    const result = await this.service.fileUpload(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => FileUpload)
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "create",
    possession: "any",
  })
  async createFileUpload(
    @graphql.Args() args: CreateFileUploadArgs
  ): Promise<FileUpload> {
    return await this.service.createFileUpload({
      ...args,
      data: {
        ...args.data,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => FileUpload)
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "update",
    possession: "any",
  })
  async updateFileUpload(
    @graphql.Args() args: UpdateFileUploadArgs
  ): Promise<FileUpload | null> {
    try {
      return await this.service.updateFileUpload({
        ...args,
        data: {
          ...args.data,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => FileUpload)
  @nestAccessControl.UseRoles({
    resource: "FileUpload",
    action: "delete",
    possession: "any",
  })
  async deleteFileUpload(
    @graphql.Args() args: DeleteFileUploadArgs
  ): Promise<FileUpload | null> {
    try {
      return await this.service.deleteFileUpload(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(@graphql.Parent() parent: FileUpload): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
